# wrangler.toml — Cloudflare Pages build configuration
# Replace "witchesbrew" with your actual project name.
# Use pages_build_output_dir — NOT [site] bucket (that's Workers Sites).
name = "witchesbrew"
compatibility_date = "2025-01-01"
pages_build_output_dir = "./dist"
